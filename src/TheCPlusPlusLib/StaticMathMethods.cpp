#include "pch.h"
#include "StaticMathMethods.h"

void Sum(int number1, int number2, int& result)
{
	result = number1 + number2;
}

void Multiply(int number1, int number2, int& result)
{
	result = number1 * number2;
}